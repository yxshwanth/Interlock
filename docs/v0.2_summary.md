# v0.2 Summary — Usable Tool Milestone Complete

**Status:** All four v0.2 phases merged to `main`; post-release HTTP overhead in v0.2.1  
**Milestone:** [v0.2 — Usable Tool](ROADMAP.md)  
**Releases:** [`v0.2.0`](https://github.com/yxshwanth/Interlock/releases/tag/v0.2.0) (milestone), [`v0.2.1`](https://github.com/yxshwanth/Interlock/releases/tag/v0.2.1) (HTTP overhead A+C)

| Phase | PR | Issue | Merged |
|---|---|---|---|
| 1 — HTTP/SSE transport | [#8](https://github.com/yxshwanth/Interlock/pull/8) | [#4](https://github.com/yxshwanth/Interlock/issues/4) | yes |
| 2 — Multi-session concurrency | [#9](https://github.com/yxshwanth/Interlock/pull/9) | [#5](https://github.com/yxshwanth/Interlock/issues/5) | yes |
| 2 review hardening | [#10](https://github.com/yxshwanth/Interlock/pull/10) | — | yes |
| 3 — Bounded encoding overlap | [#12](https://github.com/yxshwanth/Interlock/pull/12) | [#11](https://github.com/yxshwanth/Interlock/issues/11) | yes |
| 4 — Benchmarks + persistence | [#14](https://github.com/yxshwanth/Interlock/pull/14) | [#13](https://github.com/yxshwanth/Interlock/issues/13) | yes |
| Post-release — HTTP overhead A+C | [#17](https://github.com/yxshwanth/Interlock/pull/17) | [#7](https://github.com/yxshwanth/Interlock/issues/7) | yes (v0.2.1) |

---

## Goal (from ROADMAP)

> Close the five gaps that separate an impressive demo from something someone can actually run against a real agent: **real transport**, **concurrency**, non-trivial detection, performance numbers, persistent evidence.

**v0.2 exit state (ROADMAP):** works on HTTP/SSE, handles concurrent sessions, catches encoded exfil, has published overhead numbers, persists evidence.

### Roadmap satisfaction audit

| Exit criterion | Met? | Evidence |
|---|---|---|
| Works on HTTP/SSE | **Yes** | Streamable HTTP 2025-11-25; `make demo-http`, `TestDemoHTTP_VariantA_Block` |
| Handles concurrent sessions | **Yes** | `SessionManager`, `PIDRegistry`, `TestConcurrentDualSession_VariantA_Block`, `make demo-http-concurrent` |
| Catches encoded exfil | **Yes** | Canonical encoding overlap; `TestCheckOverlap_EncodedExfil_KnownGap` passes |
| Published overhead numbers | **Yes** (scoped) | [`docs/performance.md`](performance.md): engine microbenchmarks (`make bench`) + end-to-end HTTP engine delta A+C (`make bench-http`, v0.2.1). Headline: **~0.5 ms sensitive reads, ~0.1 ms sink checks** (sub-ms). Concurrent multi-session load added post-v0.2 (`TestHTTP_ConcurrentLoad_ReadTicket`) |
| Persists evidence | **Yes** | JSONL default (**intentional** — demo/dev-friendly append); SQLite opt-in (`evidence.backend: sqlite` + `max_records`) for bounded restart-safe retention |

| Original “five gaps” | Met? | Notes |
|---|---|---|
| Real transport | **Yes** | Phase 1 |
| Concurrency | **Yes** | Phase 2 + race CI (#10) |
| Non-trivial detection | **Yes** (asymmetric) | Variant A: encoding-aware EXFIL. Variant B (post-v0.2): `SUSPICIOUS` on connect-only; `EXFIL` when correlated `write()` excerpt overlaps taint. `sendto`/UDP still open |
| Performance numbers | **Yes** | Engine + single-session HTTP delta (v0.2.1); concurrent multi-session absolute p99 (`TestHTTP_ConcurrentLoad_ReadTicket`); eBPF DropCount CI + root-gated saturation |
| Persistent evidence | **Yes** (intentional default) | JSONL default by design; SQLite opt-in when operators want retention. Not a gap — a posture choice |

**Verdict:** The v0.2 **exit state is satisfied** for the scoped claims documented in README limitations and known-gap tests. Post-v0.2 closed the Variant B payload gap (dual claim), concurrent HTTP load characterization, and ringbuf DropCount/saturation testing. Evidence default remains **JSONL by intention** (SQLite opt-in for retention) — not an unfinished persistence story. Quote the **engine delta (C)** for Interlock overhead; absolute end-to-end p99 is backend-dominated.

---

## Phase 1 — HTTP/SSE Transport Interception

### Done when

> Full trifecta demo runs against an HTTP MCP server, not just STDIO. — **Met**

### Shipped

- Streamable HTTP ([MCP 2025-11-25](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports/streamable-http)): `POST /mcp`, `Mcp-Session-Id`, JSON + SSE
- `internal/proxy/dispatch.go` — transport-agnostic dispatch shared with STDIO
- `internal/proxy/http/` — transport, headers, response, client
- **Inspect-then-forward** — full JSON-RPC body before policy; SSE only after complete backend response; blocked calls return JSON immediately
- Config: `transport.mode`, `listen`, `endpoint`, `protocol_version`, `prefer_sse_responses`
- Demos: `make demo-http`, `make demo-http-ebpf`, quiet variants
- Example configs: `interlock-http.yaml`, `interlock-http-monitor.yaml`

### Scope decisions

- Agent-facing HTTP only; **backends stay STDIO** (eBPF PID dependency)
- Bind `127.0.0.1` — inside trust boundary; TLS/MITM deferred
- STDIO unchanged and still default

### Deferred (not v0.2)

- HTTP upstream MCP backends
- GET `/mcp` listen streams, 2026-07-28 stateless protocol

---

## Phase 2 — Multi-Session Concurrency and Attribution

### Done when

> Two poisoned sessions run concurrently, each correctly attributed, neither leaking state. — **Met**

### Shipped

- `SessionManager` + `SessionRuntime` — per-session backend pools spawned on HTTP `initialize`
- `PIDRegistry` — `(pid, /proc start_time)` → `{session_id, server_id}`
- eBPF dynamic `AddPID` / `RemovePID` on session spawn and cleanup
- Removed `FirstSessionID`; `IngestSyscall` requires explicit `SessionID`
- Unattributed eBPF events: `[SECURITY]` log + `SecurityAuditEvent` in `events.jsonl` (#10)
- Config: `sessions.max_concurrent` (32), `sessions.idle_timeout` (30m)
- Tests: `TestConcurrentDualSession_VariantA_Block`, PID reuse, session isolation
- Demo: `make demo-http-concurrent`

### Review hardening (#10)

- CI **`race` job** — `go test -race` on `./internal/proxy/...` and `./internal/engine/...`
- Concurrent overlap barriers in dual-session and `SessionManager` tests
- DoS limitation documented (per-session pool bound)
- `TestSessionStore_ConcurrentAccess` rewritten to exercise `Engine` under `-race`

### Honest limitations

- Per-session pools = bounded DoS surface (`max_concurrent × servers`)
- Just-died PID window: audit-logged, not tripped
- Shared `PIDRegistry` / `SessionManager` — race CI is the safety net

---

## Phase 3 — Bounded Encoding Overlap (Dataflow Taint)

### Done when

> `TestCheckOverlap_EncodedExfil_KnownGap` passes — base64'd token in sink caught. — **Met**

### Shipped

- Fixed transform set at taint registration: **literal, base64, hex, URL-encoding, reversal**
- `CheckOverlap` scans all canonical forms; evidence records `match_form`
- `RedactJSON` scrubs encoded variants from logs
- Engine integration: encoded Variant A exfil → `EXFIL` (0.95)

### Known-gap tests (what we still don't catch)

| Test | Gap |
|---|---|
| `TestCheckOverlap_SplitAcrossCalls_KnownGap` | Secret split across fields/calls |
| `TestCheckOverlap_Compressed_KnownGap` | gzip/compressed exfil |
| `TestCheckOverlap_DoubleEncoded_KnownGap` | Nested encoding (e.g. base64(hex)) |

### Deferred

- eBPF `sendto`/`write` payload capture — Variant B `EXFIL` upgrade (0.95 with payload proof)

---

## Phase 4 — Performance, Benchmarks, and Persistent Evidence

### Done when

> Published overhead numbers + evidence survives restart without unbounded growth. — **Met** (scoped)

### Shipped

- Engine benchmarks: `internal/engine/bench_test.go`, `make bench`
- End-to-end HTTP overhead (v0.2.1): `TestHTTP_OverheadReport_*`, `BenchmarkHTTP_EngineDelta_*`, `make bench-http`, CI smoke (`OVERHEAD_SAMPLES=100`)
- [`docs/performance.md`](performance.md) — engine microbenchmarks + HTTP delta methodology; headline sub-ms engine overhead
- CI benchmark smoke (compile/run, no threshold gate)
- **SQLite evidence (opt-in):** `evidence.backend: sqlite`, `max_records` retention; restart + **concurrent retention** tests (`TestSQLiteEvidenceSink_ConcurrentRetention` runs under race CI via `./internal/engine/...`)
- **JSONL intentional default** — demo/dev-friendly append; bounded retention is opt-in by design (not a deferred gap)
- **Backpressure:** `logging.backpressure: block | drop`; `RuntimeStats` at shutdown
- **eBPF ring-buffer drops:** `drop_count` BPF map; `Sensor.DropCount()`

### Performance snapshot (see performance.md for methodology)

**Engine delta (C) — HTTP proxy overhead (v0.2.1):**

| Benchmark | Delta | Notes |
|---|---:|---|
| `BenchmarkHTTP_EngineDelta_ReadTicket` | **~536 µs** (+63 allocs) | Taint ingestion on sensitive read; 2-secret fixture |
| `BenchmarkHTTP_EngineDelta_MonitorSinkBenign` | **~118 µs** (+32 allocs) | Overlap check on sink; no new taint registration |

**Engine microbenchmarks:**

| Benchmark | ns/op | Notes |
|---|---:|---|
| `CheckOverlap` (1 tainted) | 70 | 5 forms per secret |
| `CheckOverlap` (50 tainted) | 2146 | Linear scaling |
| `IngestResult` taint extract | ~8163 | Registration + encodings (post-v0.2 mechanical opts; was ~14860) |
| `EvaluateRequest` exfil block | 562798 | Worst-case trip path incl. evidence emit — rare only |

### Known-gap tests

| Test | Gap |
|---|---|
| `TestHTTP_ConcurrentLoad_ReadTicket` | **Closed post-v0.2** — multi-session absolute p99 published |
| `TestEBPF_RingbufSaturation_UnderLoad` | **Closed post-v0.2** — CI DropCount API; saturation root-gated |
| `TestEvidenceStore_CrossSessionQuery_KnownGap` | No query API / viewer DB integration |
| `TestEventLogger_DiskFull_KnownGap` | Disk-full behavior undefined |

### Deferred

- Prometheus / metrics HTTP (v0.3)
- SQLite for `events.jsonl`

---

## Architecture (v0.2 end state)

```
Agent(s) ── HTTP POST /mcp ──► SessionManager ──► SessionRuntime (per session)
                                      │                    │
                                      │                    ├── STDIO backends (tickets/messenger/exfil)
                                      │                    └── PIDRegistry ──► eBPF pid_filter
                                      ▼
                                 engine (per session_id)
                                   ├── trifecta state machine
                                   ├── CheckOverlap (5 forms × N tainted)
                                   └── EvidenceSink (JSONL intentional default | SQLite opt-in retention)
                                      ▲
                                      │ SyscallEvent + SessionID
                                 eBPF sensor (connect + drop_count)
```

Two planes, one engine — session-aware on both paths, encoding-aware on Variant A.

---

## Test Results (current)

- **110 tests passing**, **7 skipped** (all known-gap documentation)
- **CI:** `test` + `race` jobs green on every push to `main`
- **Race coverage:** `./internal/proxy/...`, `./internal/engine/...`
- **Integration:** HTTP transport, concurrent dual-session (require `make build` for server binaries)

Skipped tests name what v0.2 explicitly does **not** claim to catch.

---

## Demo Targets

| Command | What it exercises |
|---|---|
| `make demo` | STDIO trifecta (v0.1 path, unchanged) |
| `make demo-http` | HTTP trifecta — monitor + block over Streamable HTTP |
| `make demo-http-ebpf` | Variant B over HTTP (requires root) |
| `make demo-http-concurrent` | Two parallel HTTP sessions, distinct evidence |
| `make demo-quiet-http` | Recording-friendly HTTP demo |
| `make bench` | Engine hot-path benchmarks |

---

## Configuration surface (v0.2 additions)

```yaml
transport:
  mode: http              # stdio | http
  listen: 127.0.0.1:8080
  endpoint: /mcp

sessions:
  max_concurrent: 32
  idle_timeout: 30m

evidence:
  backend: jsonl          # jsonl (default) | sqlite
  path: evidence.jsonl
  max_records: 1000       # sqlite only

logging:
  backpressure: block     # block | drop
  queue_size: 256
```

---

## What's next

**Released:** `v0.2.0` (milestone) and `v0.2.1` (HTTP overhead). See [CHANGELOG.md](../CHANGELOG.md).

**Post-v0.2 backlog** (see [`task_list.md`](task_list.md) / [`ROADMAP.md`](ROADMAP.md)):

- ~~Async evidence emit~~ / ~~taint ingestion opts~~ / ~~concurrent HTTP load~~ / ~~eBPF write payload EXFIL~~ / ~~ringbuf DropCount tests~~ — landed
- Remaining: `openat`/DNS probes, tool-shadowing; Prometheus / K8s / LSM (v0.3 arc)

---

## References

- [ROADMAP.md — v0.2](ROADMAP.md)
- [architecture.md](architecture.md)
- [performance.md](performance.md)
- [CHANGELOG.md](../CHANGELOG.md)
- Prior build sequence: [week1_summary.md](week1_summary.md), [week2_summary.md](week2_summary.md), [week3_summary.md](week3_summary.md) (v0.1)
